import { TypeDBClient as TypeDBClient } from "./api/connection/TypeDBClient";
import { TypeDBCredential } from "./api/connection/TypeDBCredential";
export declare namespace TypeDB {
    const DEFAULT_ADDRESS = "127.0.0.1:1729";
    function coreClient(address?: string): TypeDBClient;
    function clusterClient(addresses: string | string[], credential: TypeDBCredential): Promise<TypeDBClient.Cluster>;
}
