import { Database } from "../../api/connection/database/Database";
import { DatabaseManager } from "../../api/connection/database/DatabaseManager";
import { TypeDBDatabaseManagerImpl } from "../TypeDBDatabaseManagerImpl";
import { ClusterClient } from "./ClusterClient";
export declare class ClusterDatabaseManager implements DatabaseManager.Cluster {
    private readonly _databaseManagers;
    private readonly _client;
    constructor(client: ClusterClient);
    contains(name: string): Promise<boolean>;
    create(name: string): Promise<void>;
    get(name: string): Promise<Database.Cluster>;
    all(): Promise<Database.Cluster[]>;
    databaseManagers(): {
        [address: string]: TypeDBDatabaseManagerImpl;
    };
    rpcClient(): ClusterClient;
    private failsafeTask;
}
