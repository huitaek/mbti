import { Database } from "../api/connection/database/Database";
import { DatabaseManager } from "../api/connection/database/DatabaseManager";
import { TypeDBStub } from "../common/rpc/TypeDBStub";
export declare class TypeDBDatabaseManagerImpl implements DatabaseManager {
    private readonly _stub;
    constructor(client: TypeDBStub);
    get(name: string): Promise<Database>;
    create(name: string): Promise<void>;
    contains(name: string): Promise<boolean>;
    all(): Promise<Database[]>;
    stub(): TypeDBStub;
}
