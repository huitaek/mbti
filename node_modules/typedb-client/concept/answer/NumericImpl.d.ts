import { Numeric as NumericProto } from "typedb-protocol/common/answer_pb";
import { Numeric } from "../../api/answer/Numeric";
export declare class NumericImpl implements Numeric {
    private readonly _value?;
    constructor(value?: number);
    asNumber(): number;
    isNaN(): boolean;
    isNumber(): boolean;
    toString(): string;
}
export declare namespace NumericImpl {
    function of(numericProto: NumericProto): NumericImpl;
}
