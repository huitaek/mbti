export declare class ResponseCollector<T> {
    private readonly _response_queues;
    constructor();
    queue(uuid: string): ResponseCollector.ResponseQueue<T>;
    get(uuid: string): ResponseCollector.ResponseQueue<T>;
    remove(requestId: string): void;
    close(error?: Error | string): void;
}
export declare namespace ResponseCollector {
    class ResponseQueue<T> {
        private readonly _queue;
        constructor();
        take(): Promise<T>;
        put(element: T): void;
        close(error?: Error | string): void;
    }
}
