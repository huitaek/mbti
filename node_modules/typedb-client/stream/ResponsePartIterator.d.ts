import { Transaction } from "typedb-protocol/common/transaction_pb";
import { BatchDispatcher } from "./RequestTransmitter";
import { ResponseCollector } from "./ResponseCollector";
export declare class ResponsePartIterator implements AsyncIterable<Transaction.ResPart> {
    private readonly _requestId;
    private readonly _responseCollector;
    private readonly _dispatcher;
    constructor(requestId: string, responseCollector: ResponseCollector.ResponseQueue<Transaction.ResPart>, dispatcher: BatchDispatcher);
    [Symbol.asyncIterator](): AsyncIterator<Transaction.ResPart, any, undefined>;
    next(): Promise<Transaction.ResPart>;
}
